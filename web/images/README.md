# web/images

Header assets for the playground.

- **`logo.png`** — the brand logo shown at the left of the header
  (`index.html` → `.brand-block .logo`). Not yet committed; a temporary QR
  code will be dropped in here. Until then the header shows a neutral
  placeholder square. Drop the image in as `logo.png` (square works best;
  it's rendered at ~28px and `object-fit: contain`).
