## Summary

<!-- Describe the change and intent. -->

## Specification

<!-- Which SPECIFICATION.html section(s) does this implement or amend? Spec-first:
     if behavior changed, the spec changed too (or say why not applicable). -->

## Checklist

- [ ] Relevant `SPECIFICATION.html` section(s) identified.
- [ ] `cargo fmt --check` (in `rust/`)
- [ ] `cargo clippy --all-targets -- -D warnings` (in `rust/`)
- [ ] `cargo test --all-targets` (in `rust/`)
- [ ] New/changed behavior is covered by tests.
